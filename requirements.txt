# Angel Memory Plugin Dependencies

# 核心依赖（轻量级）
chromadb>=1.2.1
requests>=2.28.0
mistune>=2.0.0
markitdown[all]>=0.1.0
tiktoken>=0.4.0
bm25s>=0.2.0
jieba>=0.42.0

# 本项目依赖的llm_memory模块已包含在插件内部
# 无需额外依赖，使用AstrBot内置的模块
